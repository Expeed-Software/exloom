# IDEs
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Local envs
.env
.env.local

# Node (for any future tooling)
node_modules/

# Python (for any future tooling)
__pycache__/
*.pyc
.venv/
venv/
.exloom/

# Session working artifacts. These are notes about *this* repo's development —
# review checklists, plans, QA sheets — and they carry branch names, ticket
# numbers and incident detail that have no business in a public repository.
# Keep them local; the plugin itself ships under plugins/.
.claude/
